# Politics-Election
It is a election site for Green Party (frictional party).
I used HTML, CSS to make this website. 
In HTML sections linking with ids.
In CSS i used variables to avoid repeated properties, and i used flex and grid in CSS.
